{
  "name": "authn-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mock OIDC OP (and future SAML IdP/SP) for authn-cn — test/dev only, never for production",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}
